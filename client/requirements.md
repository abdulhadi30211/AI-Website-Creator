## Packages
framer-motion | Page transitions and high-quality micro-interactions
date-fns | Human-readable date formatting for project lists
clsx | Conditional class merging
tailwind-merge | Tailwind class deduplication

## Notes
- Sidebar assumes standard dashboard layout
- Project status polling implemented via TanStack Query `refetchInterval`
- Iframe sandbox assumes safe HTML/CSS/JS execution environment
- Wouter handles client-side routing
